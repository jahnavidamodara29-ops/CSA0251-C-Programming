#include <stdio.h>

int main() {
    int M, N, K;

    printf("Enter M, N and K: ");
    scanf("%d %d %d", &M, &N, &K);

    for(int i = M; i <= N; i += (K + 1)) {
        printf("%d ", i);
    }

    return 0;
}
