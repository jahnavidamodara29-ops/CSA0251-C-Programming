#include <stdio.h>

int main() {
    int m, n;

    printf("Enter number (m): ");
    scanf("%d", &m);

    printf("Enter limit (n): ");
    scanf("%d", &n);

    for(int i = 1; i <= n; i++) {
        printf("%d X %d = %d\n", i, m, i * m);
    }

    return 0;
}
