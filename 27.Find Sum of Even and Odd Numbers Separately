#include <stdio.h>
int main() {
    int n=10, even=0, odd=0;
    for(int i=1;i<=n;i++){
        if(i%2==0) even+=i;
        else odd+=i;
    }
    printf("Even=%d Odd=%d", even, odd);
    return 0;
}
